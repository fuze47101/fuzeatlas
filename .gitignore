.env
.vercel
node_modules

# Next.js
.next/

# Next.js
.next
out

# deps
node_modules

# env
.env.local

# Next.js
.next
out

# deps
node_modules

# logs
*.log
.env*.local

# Pre-migration cleanup 2026-05-06
tsconfig.tsbuildinfo
.scratch-*
**/.scratch-*
test-classifier.mjs
.claude/
.vercel-prod.env
.vercel/

.env.local
.env*.bak*
.env.local.bak*

# Generated/tracked-by-mistake artifacts (2026-07)
# NOTE: use `deliverables/*` (glob) not `deliverables/` — a bare directory
# ignore blocks negation of any file inside per gitignore semantics.
deliverables/*
# But keep the append-only competitor-SDS audit transcripts — those are
# the CLAUDE.md-documented source-of-truth for every competitive
# deep-dive and MUST be committed.
!deliverables/Competitor_SDS_Audit_*.md
sprayer-comparison.html
~$*
*.bak
