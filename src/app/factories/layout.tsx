import type { Metadata } from "next";
export const metadata: Metadata = { title: "Factories" };
export default function FactoriesLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
