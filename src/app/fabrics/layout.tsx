import type { Metadata } from "next";
export const metadata: Metadata = { title: "Fabrics" };
export default function FabricsLayout({ children }: { children: React.ReactNode }) { return <>{children}</>; }
